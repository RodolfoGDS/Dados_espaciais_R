#rgdal
#raster

## instalando os pacotes
install.packages('rgdal')
install.packages('raster')

## Fonte dos dados SRTM
# https://www.cnpm.embrapa.br/projetos/relevobr/
# https://srtm.csi.cgiar.org/

# Carregando as bibliotecas

library(raster)

## Atribuindo a variavel relevo o caminho do arquivo TIF 
relevo <- raster('C:/Users/rodolfo.paula/Documents/MeusProjetos/Dados_espaciais_R/SRTM/SF-23-Z-B.tif')
plot(relevo)
